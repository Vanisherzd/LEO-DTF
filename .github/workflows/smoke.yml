name: LEO-DTF Smoke Tests

on:
  push:
    branches: [main]
  pull_request:

jobs:
  smoke:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    env:
      PYTHONPATH: src
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install package and test dependencies
        run: |
          python -m pip install --upgrade pip
          python -m pip install -e .
          python -m pip install pytest

      - name: Run smoke test
        run: python scripts/run_smoke_test.py

      - name: Validate repository state (fast)
        run: python scripts/validate_repo_state.py

      - name: Check paper LaTeX sanity
        run: python scripts/check_paper_sanity.py

      - name: List placeholder citations
        run: python scripts/list_placeholder_citations.py

      - name: Run tests
        run: pytest -q